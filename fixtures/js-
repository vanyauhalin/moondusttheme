// There is no scope for the constructor in the JavaScript grammar.
// p0("class {"), p3("constructor"), p0("() {}}")
class {constructor() {}}

// JavaScript TextMate grammar has no rule for the `of` non-keyword.
const of = ""

// It is not a part of the core javascript grammar.
// https://github.com/0x00000001A/es6-string-html/issues/51/
/*css*/``
