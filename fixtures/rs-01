// Rust grammar has no rule for an exclamation mark in a macro invocation.
a!();

// Rust grammar has no rule for a type annotation.
struct a {a: str}
fn a(a: str, b: str) -> A {}
let a: fn(str, str) -> str = a;
